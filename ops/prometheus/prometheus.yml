global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: clever_agents_api
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:8000

